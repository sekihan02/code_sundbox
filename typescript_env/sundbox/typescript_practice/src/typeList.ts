// Typescript 基本の型

// bool
let bool: boolean = true;

// number
let num: number = 0;

// string
let str: string = "Hello";

// array
let arr1: Array<number> = [0, 1, 2];
let arr2: number[] = [0, 1, 2];

// tuple
let tupple: [string, number] = ["a", 0];

// any
let any: any = false;

// void
const fnction = (): void => {
  const test = "test";
};

// null
let null1: null = null;
// undefind
let undefind1: undefined = undefined;

// object
let obj: object = {};
let obj2: { id: number; name: string } = { id: 0, name: "abc" };
